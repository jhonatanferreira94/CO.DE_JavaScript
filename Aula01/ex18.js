// Imprimir números pares de 0 a 20 utilizando o loop while
let numero = 0; //Toda varíavel que o while for avaliar, deve ser criada antes da estrutura da repeticao.

while (numero<20) {

    numero+=2;
    console.log(numero);  
}